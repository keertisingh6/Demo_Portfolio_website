services:
  app:
    image: YOUR_DOCKERHUB_USERNAME/deployment-demo:latest
    container_name: deployment-demo
    ports:
      - "13000:80"   # host:container (nginx uses 80)
    restart: unless-stopped
